
// initial state
const state = {
    login:          'user/login' ,                //登录
    upload:         'upload/uploadFile',          //上传文件    
    findall:        'course/findall',             //获取课程列表
    category:       'category/list',              //获取所有课程分类
    course:         'course/add',                 //新建课程
    getInfo:        'course/getInfo',             //获取课程详情
}

// getters
const getters = {
}

// actions
const actions = {
   
}

// mutations
const mutations = {
    
}

// for(let key in state){
//     state[key] = baseUrl + state[key]
// }

export default {
    state,
    getters,
    actions,
    mutations
}